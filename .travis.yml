language: scala

jdk: openjdk8

scala:
   - 2.10.4
   - 2.12.10

script:
   - echo BURP_SCAN_URL = http://cnn.com >> temp.log &&
     sbt compile test run >> temp.log

after_success:   
   - wget http://cnn.com > temp_burp.log &&
     less temp_burp.log

notifications:
  email: mgotur@xtivia.com